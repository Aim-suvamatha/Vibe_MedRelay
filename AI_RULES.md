# AI_RULES.md — MedRelay

กฎด้าน **AI safety · privacy · security** ของโครงการนี้
เอกสารนี้เป็นข้อผูกพัน ไม่ใช่คำแนะนำ — ทุกบรรทัดของโค้ดและทุก prompt ต้องไม่ขัดกับเอกสารนี้

---

## 0. หลักการสูงสุด 3 ข้อ

1. **ระบบนี้ไม่วินิจฉัยและไม่ตัดสินใจทางคลินิกแทนมนุษย์** — AI ในระบบทำได้แค่จัดระเบียบข้อมูลที่มนุษย์บันทึกไว้แล้ว
2. **เฟส prototype ห้ามมีข้อมูลผู้ป่วยจริงในระบบโดยเด็ดขาด** — ไม่ใช่ "ระวัง" แต่คือ "ห้าม"
3. **ถ้าไม่แน่ใจว่าข้อมูลชิ้นใดระบุตัวตนได้หรือไม่ ให้ถือว่าระบุได้** และไม่ใส่ลงระบบ

---

## 1. แยกให้ชัด: AI 2 ตัวในโครงการนี้

| | **AI Developer Assistant** | **Application AI** |
|---|---|---|
| คือใคร | Claude ที่ผู้พัฒนาใช้เขียนโค้ด | External LLM API ที่ระบบเรียกใช้ตอน runtime |
| ทำงานเมื่อไร | ตอน build ก่อน deploy | ตอนผู้ใช้กดใช้งาน |
| อยู่ใน production หรือไม่ | **ไม่อยู่** | อยู่ |
| เห็นอะไร | schema, โค้ด, mock data | เฉพาะข้อมูลที่ระบบส่งให้ตามกฎข้อ 4 |
| สถานะเฟส 1 | ใช้ตลอด | **ปิดไว้** (`NEXT_PUBLIC_FEATURE_AI_SUMMARY=false`) |

> ต้องอธิบายให้ตรงบนเวที กรรมการมักถามว่า "AI อยู่ตรงไหนของระบบ" — คำตอบคือ **AI ช่วยสร้างระบบ แต่ระบบไม่ได้ตัดสินใจแทนแพทย์**

---

## 2. กฎสำหรับ AI Developer Assistant (ตอนเขียนโค้ด)

### ห้ามเด็ดขาด

- ❌ **ห้ามวางข้อมูลผู้ป่วยจริงลงในช่องแชทกับ AI** ไม่ว่าจะอ้างว่าลบชื่อแล้วหรือไม่
- ❌ **ห้ามวาง API key, service_role key, connection string หรือ token ใดๆ ลงในแชท** — ถ้าต้องให้ AI เห็นรูปแบบ ให้ใช้ค่าสมมติจาก `.env.example`
- ❌ ห้ามวางเนื้อหาเอกสารลับหรือเอกสารชั้นความลับของหน่วยลงในแชท
- ❌ ห้ามวางรายชื่อกำลังพลจริง ทะเบียนรถราชการจริง หรือพิกัดที่ตั้งหน่วยจริง
- ❌ ห้ามคัดลอกโค้ดที่ AI สร้างเข้าโปรเจกต์โดยไม่อ่าน โดยเฉพาะโค้ดที่แตะ auth, RLS หรือ query ที่มีข้อมูลผู้ป่วย

### ต้องทำ

- ✅ ให้ AI เห็น **schema และโครงสร้าง** ได้เต็มที่ — schema ไม่ใช่ข้อมูล
- ✅ ใช้ mock data ที่เป็นชื่อสมมติเสมอเมื่อต้องยกตัวอย่างให้ AI
- ✅ **ทุกครั้งที่ AI สร้างตารางใหม่ ต้องถาม AI ต่อทันทีว่า RLS policy ของตารางนี้คืออะไร** — ตารางที่ลืมเปิด RLS คือตารางที่ใครก็อ่านได้ด้วย anon key
- ✅ review diff ก่อน commit ทุกครั้ง โดยเฉพาะไฟล์ที่ชื่อขึ้นต้นด้วย `.env` หรืออยู่ใน `supabase/`
- ✅ ถ้า AI เสนอให้ใช้ `service_role` key ในฝั่ง client ให้ปฏิเสธและถามหาทางเลือกที่ใช้ RLS แทน

---

## 3. Privacy — ข้อมูลส่วนบุคคลและ PDPA

### 3.1 สิ่งที่ระบบเก็บและไม่เก็บ

| ประเภทข้อมูล | เฟส prototype | เหตุผล |
|---|---|---|
| เลขบัตรประชาชน 13 หลัก | ❌ **ไม่เก็บ** | ตัดออกโดยเจตนา ลดความเสี่ยงและลดภาระตาม PDPA |
| ชื่อ-นามสกุลผู้ป่วยจริง | ❌ **ไม่เก็บ** | ใช้ `case_code` และ `patient_alias` แทน |
| ภาพถ่ายบาดแผลหรือใบหน้า | ❌ **ไม่เก็บ** | ระบุตัวตนได้ และไม่จำเป็นต่อการพิสูจน์แนวคิด |
| พิกัด GPS เรียลไทม์ | ❌ **ไม่เก็บ** | ใช้จุดรับผู้ป่วยที่กำหนดไว้ล่วงหน้า ซึ่งตรงหลักนิยมทางทหารมากกว่า และไม่เป็นข้อมูลตำแหน่งบุคคล |
| เลขประจำตัวทหารของ **ผู้ใช้ระบบ** | ✅ เก็บ | เป็นข้อมูลของเจ้าหน้าที่ผู้ใช้ระบบเอง ใช้ยืนยันตัวตน ไม่ใช่ข้อมูลผู้ป่วย |
| อาการสำคัญ สัญญาณชีพ ระดับ triage | ✅ เก็บ (จำลอง) | เป็นหัวใจของระบบ แต่เฟสนี้เป็นข้อมูลจำลองทั้งหมด |
| เวลาแต่ละขั้นตอน | ✅ เก็บ | ไม่ระบุตัวตน และเป็นตัวชี้วัดหลักของโครงการ |

### 3.2 บังคับด้วยโครงสร้าง ไม่ใช่ด้วยความตั้งใจ

- ตาราง `case` มีฟิลด์ `is_synthetic boolean not null default true`
- RLS policy `case_insert` บังคับ `is_synthetic = true` — **ระดับ database ปฏิเสธการ insert ข้อมูลที่ทำเครื่องหมายว่าเป็นข้อมูลจริง**
- `.env.example` มี `NEXT_PUBLIC_ALLOW_REAL_PATIENT_DATA=false` เป็นค่าเริ่มต้น
- `.gitignore` กัน `data/raw/`, `*.patient.csv`, `*.phi.*`, `seed-real-*.sql`

### 3.3 ถ้าต้องใช้ข้อมูลจริงในอนาคต (Phase 2+)

ก่อนนำข้อมูลผู้ป่วยจริงเข้าระบบแม้แต่รายเดียว ต้องครบทั้ง 5 ข้อ

1. ผ่านการพิจารณาของคณะกรรมการจริยธรรมการวิจัยในมนุษย์ หรือได้รับอนุมัติจากผู้มีอำนาจตามระเบียบของหน่วย
2. มีบันทึกข้อความอนุมัติจากผู้บังคับบัญชาตามสายงาน (ถึงระดับแม่ทัพภาคที่ 3)
3. มีข้อตกลงการประมวลผลข้อมูลกับผู้ให้บริการ (Supabase / ผู้ให้บริการ LLM)
4. ผ่านการตรวจของนายทหารพระธรรมนูญเรื่องความสอดคล้องกับ PDPA และระเบียบกองทัพ
5. มีผู้รับผิดชอบระบบที่ระบุชื่อชัดเจน ไม่ใช่ผู้พัฒนาคนเดียว

### 3.4 สิทธิของเจ้าของข้อมูล

แม้เฟสนี้ไม่มีข้อมูลจริง ระบบต้องออกแบบให้รองรับไว้แล้ว

- **สิทธิเข้าถึง** — `event_log` ทำให้ตอบได้ว่าใครดูข้อมูลเคสใดเมื่อไร
- **สิทธิแก้ไข** — `assessment` แก้ไม่ได้โดยเจตนา (เหมือนเวชระเบียนกระดาษ) การแก้ทำโดยบันทึกการประเมินใหม่ ไม่ใช่ทับของเดิม
- **สิทธิลบ** — ต้องมีขั้นตอนลบที่ตรวจสอบได้ ยังไม่ implement ในเฟส 1

---

## 4. กฎสำหรับ Application AI (ตอน runtime)

### 4.1 ขอบเขตที่อนุญาต

AI ในแอปทำได้เฉพาะ

- ✅ **สรุปข้อความส่งมอบผู้ป่วย** จากข้อมูลที่มนุษย์บันทึกไว้แล้ว ให้อ่านง่ายขึ้น
- ✅ **จัดรูปแบบรายงาน** จากข้อมูลในฐานข้อมูล
- ✅ **ตรวจความครบถ้วนของฟอร์ม** เช่น เตือนว่ายังไม่ได้บันทึกสัญญาณชีพ

### 4.2 ห้ามเด็ดขาด

- ❌ **ห้ามวินิจฉัยโรค** ไม่ว่าจะเป็นการเสนอแนะหรือการจัดอันดับความน่าจะเป็น
- ❌ **ห้ามกำหนดหรือแนะนำระดับ triage** — triage เป็นการตัดสินใจทางคลินิกของมนุษย์เท่านั้น
- ❌ **ห้ามจัดลำดับความเร่งด่วนของเคส** (precedence) แทนศูนย์สั่งการ
- ❌ **ห้ามแนะนำการรักษา ขนาดยา หรือหัตถการ**
- ❌ ห้ามตัดสินใจจัดสรรทรัพยากร (รถ/ชุด) โดยอัตโนมัติ
- ❌ ห้ามเขียนข้อมูลลงตาราง `assessment` โดยตรง — AI เสนอข้อความได้ มนุษย์เป็นผู้กดยืนยันและเป็นเจ้าของบันทึกเสมอ

> **เหตุผล** — MASCAL triage engine เป็น moat ที่มีค่าที่สุดของโครงการ แต่ตัดออกจากเฟส 1 โดยเจตนาเพราะ **ต้อง validate ทางคลินิกก่อนใช้จริง** การปล่อย AI แนะนำ triage โดยไม่ validate คือความเสี่ยงต่อผู้ป่วยและต่อผู้พัฒนาในฐานะแพทย์

### 4.3 กฎทางเทคนิคของการเรียก LLM

| กฎ | รายละเอียด |
|---|---|
| **Server-side only** | เรียกจาก Route Handler / Server Action เท่านั้น `LLM_API_KEY` ห้ามขึ้นต้นด้วย `NEXT_PUBLIC_` |
| **Data minimisation** | ส่งเฉพาะฟิลด์ที่จำเป็นต่องานนั้น ห้ามส่งทั้ง row ห้ามส่ง `service_number`, `phone`, `patient_alias` |
| **No training** | ต้องเลือกผู้ให้บริการและตั้งค่าที่ไม่นำข้อมูลไปเทรน และบันทึกไว้ว่าเลือกอย่างไร |
| **Human in the loop** | ผลลัพธ์ทุกชิ้นต้องแสดงให้มนุษย์ตรวจก่อนบันทึก ไม่มี auto-commit |
| **แสดงที่มา** | UI ต้องระบุชัดว่าข้อความนี้สร้างโดย AI เช่น badge "ร่างโดย AI — กรุณาตรวจสอบ" |
| **Fail closed** | ถ้า LLM error หรือ timeout ระบบต้องทำงานต่อได้โดยไม่มี AI ไม่ใช่ค้างทั้งหน้า |
| **Rate limit + timeout** | ตั้ง timeout และจำกัดจำนวนครั้งต่อผู้ใช้ ป้องกันค่าใช้จ่ายบานปลาย |
| **Log การเรียก** | บันทึกใน `event_log` ว่ามีการเรียก AI เมื่อไร โดยใคร กับเคสใด (ไม่บันทึกเนื้อหา prompt ที่มีข้อมูลผู้ป่วย) |

---

## 5. Security

### 5.1 Key management

| Key | ที่อยู่ | เปิดเผยได้ |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | browser + server | ✅ ได้ |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | browser + server | ✅ ได้ (RLS คุมอีกชั้น) |
| `SUPABASE_SERVICE_ROLE_KEY` | **server เท่านั้น** | ❌ ข้ามทุก RLS |
| `LLM_API_KEY` | **server เท่านั้น** | ❌ |

**ถ้า key หลุดขึ้น GitHub** — ลำดับที่ถูกต้องคือ

1. **revoke/rotate key ทันที** ที่หน้า dashboard ของผู้ให้บริการ
2. ค่อยลบออกจาก git history
3. ตรวจ log การใช้งานย้อนหลัง

> การลบ commit อย่างเดียวไม่ช่วย เพราะ key ถูก scan เก็บไปแล้วภายในไม่กี่วินาที

### 5.2 Checklist ก่อน commit ทุกครั้ง

- [ ] `git status` ไม่มีไฟล์ `.env*` (ยกเว้น `.env.example`)
- [ ] `git diff` ไม่มีสตริงที่ขึ้นต้นด้วย `eyJ`, `sk-`, `sbp_`, `postgres://`
- [ ] ไม่มีชื่อจริงของผู้ป่วยหรือกำลังพลใน seed / test / comment
- [ ] ตารางใหม่ทุกตารางมี `enable row level security` และมี policy อย่างน้อย 1 ข้อ
- [ ] ไม่มี `service_role` ในไฟล์ที่มี `"use client"`

คำสั่งตรวจเร็ว

```bash
git diff --cached | grep -nE 'eyJ[A-Za-z0-9_-]{10,}|sk-[A-Za-z0-9]{10,}|postgres://|service_role' && echo "⚠️  พบ secret — หยุด commit" || echo "✅ ผ่าน"
```

### 5.3 หลักการที่ไม่ต่อรอง

- **บังคับสิทธิ์ที่ชั้น database เสมอ** — การซ่อนปุ่มบนหน้าจอไม่ใช่ security
- **Validate ทั้งสองฝั่ง** — client validate เพื่อ UX, server validate เพื่อความปลอดภัย
- **RLS เปิดทุกตารางโดยไม่มีข้อยกเว้น** รวมถึงตารางที่ "ไม่มีข้อมูลสำคัญ"
- **Audit trail แก้ไม่ได้** — `event_log` และ `assessment` ไม่มี policy สำหรับ UPDATE/DELETE

---

## 6. ความปลอดภัยเชิงคลินิก (Clinical Safety)

| ความเสี่ยง | มาตรการ |
|---|---|
| ผู้ใช้เชื่อข้อมูลในระบบแทนการประเมินผู้ป่วยตรงหน้า | UI ต้องระบุว่าข้อมูลเป็น "ผลประเมินจากทอดก่อนหน้า" พร้อมเวลาที่ประเมิน ไม่ใช่สภาพปัจจุบัน |
| ระบบล่มหรือไม่มีสัญญาณระหว่างภารกิจ | **ต้องใช้วิทยุคู่ขนานเสมอ** ระบบเป็นชั้นบันทึก ไม่ใช่ช่องทางสั่งการเดียว — ข้อนี้ต้องเขียนในคู่มือผู้ใช้และพูดบนเวที |
| ข้อมูลเก่าถูกเข้าใจว่าเป็นข้อมูลปัจจุบัน | ทุกค่าที่แสดงต้องมี timestamp กำกับ |
| ประเมินผิดแล้วแก้ไม่ได้ | โดยเจตนา — ให้บันทึกการประเมินใหม่ต่อท้าย เหมือนเวชระเบียนกระดาษ ทำให้ตรวจสอบย้อนหลังได้ |
| AI สรุปข้อความผิดเพี้ยน | ต้องแสดงข้อความต้นฉบับคู่กับข้อความที่ AI สรุปเสมอ |

---

## 7. สถานะทางกฎหมายของโครงการ

- พัฒนา **นอกเวลาราชการ** บนเครื่องและบัญชีส่วนตัว ไม่ใช้ระบบหรืองบประมาณของหน่วย
- ลิขสิทธิ์เป็นของแพทย์ผู้พัฒนาตามที่หลักสูตรระบุ · ยินดีให้กองทัพใช้โดยไม่คิดค่าลิขสิทธิ์หากประสงค์จะนำไปใช้
- การทดสอบกับกำลังพลเป็น **การขอความร่วมมือโดยสมัครใจนอกเวลา ไม่ใช่การสั่งการ** และต้องบันทึกไว้ให้ชัดเจน
- บันทึกข้อความเสนอถึงระดับแม่ทัพภาคที่ 3 ควรผ่านการตรวจของนายทหารพระธรรมนูญก่อนเสนอ

---

## 8. สิ่งที่ต้องพูดบนเวที (เตรียมคำตอบไว้ล่วงหน้า)

| คำถามที่จะถูกถาม | คำตอบ |
|---|---|
| "AI อยู่ตรงไหนของระบบ" | AI ช่วยผมสร้างระบบ แต่ระบบไม่ได้ตัดสินใจแทนแพทย์ — ฟีเจอร์ AI ในแอปยังปิดอยู่ในเฟสนี้โดยเจตนา |
| "ข้อมูลผู้ป่วยปลอดภัยไหม" | เฟสนี้ไม่มีข้อมูลผู้ป่วยจริงในระบบเลย และ database ปฏิเสธการ insert ข้อมูลที่ไม่ได้ทำเครื่องหมายว่าเป็นข้อมูลจำลอง |
| "PDPA จัดการอย่างไร" | ตัดเลขบัตรประชาชนออกตั้งแต่ออกแบบ ใช้รหัสเคสแทนตัวระบุผู้ป่วย และมี audit trail ที่แก้ไม่ได้ |
| "ถ้าไม่มีสัญญาณล่ะ" | ใช้วิทยุคู่ขนานเสมอ ระบบเป็นชั้นบันทึกไม่ใช่ชั้นสั่งการเดียว และ offline queue อยู่ในแผนเฟส 2 ซึ่ง schema รองรับไว้แล้ว |
| "ทำไมไม่ให้ AI ช่วย triage" | เพราะยังไม่ผ่านการ validate ทางคลินิก การปล่อยให้ AI แนะนำ triage โดยไม่ validate คือความเสี่ยงต่อผู้ป่วย |
| "ใครดูแลระบบต่อ" | ระยะนำร่องแผนกแพทย์ ทภ.3 ระยะขยายผลเสนอกรมแพทย์ทหารบก — ไม่ใช่ผมคนเดียว |
